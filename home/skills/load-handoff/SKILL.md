---
name: load-handoff
description: "Use this skill when the user wants to resume work from a previous handoff file. Trigger on requests like \"continue from handoff\", \"resume work\", \"load handoff\", \"resume from handoff\", or when the user provides a handoff file path. Use when the user wants to continue previous work without losing context. The user can provide a handoff file path as an argument, either as a relative path or using @ file reference (e.g., /load_handoff .codex/handoffs/YYMMDD-task-name/HANDOFF.md or /load_handoff @HANDOFF.md). Do NOT use for general file reading or tasks unrelated to resuming previous work."
---

# Resume Work from Handoff

## Purpose

Load a handoff file from a previous session to restore the work context and continue from where it was left off.

## Prerequisites

- User must provide a handoff file path
- Handoff file must follow the standard format (generated by the handoff skill)

## Workflow

1. **Parse File Path**:
   - Check the path provided in args
   - If absolute path: use as-is
   - If relative path: resolve relative to workspace root
   - If passed via @ file reference: already converted to absolute path
2. **Read Handoff File**: Read the handoff file at the processed path
3. **Analyze Context**:
   - Task Overview: Understand the work goal
   - Current Progress: Check completed and in-progress tasks
   - Identified Issues: Review discovered problems
   - Key File Modifications: Check file modification status
   - Notes: What worked and what problems occurred
   - Next Steps: What needs to be done next
4. **Summarize Status**: Provide the user with a summary of current state
5. **Propose Next Action**: Ask if they want to start the Next Steps immediately
6. **Resume Work**: Execute Next Steps tasks after user approval

## Usage

### Method 1: Using @ File Reference (Recommended)

Type `@` in VSCode to trigger the file selection UI:

```bash
/load_handoff @HANDOFF-2026-02-10-143045.md
```

**Benefits**:
- Easy file selection via UI
- Filename auto-completion support
- Automatically converted to absolute path

### Method 2: Using Relative Path

```bash
/load_handoff .codex/handoffs/260210-task-name/HANDOFF-2026-02-10-143045.md
```

Uses a path relative to the workspace root.

### Method 3: Using Absolute Path

```bash
/load_handoff /home/user/workspace/.codex/handoffs/260210-task-name/HANDOFF-2026-02-10-143045.md
```

### Skill Invocation

```
skill: "load-handoff"
args: ".codex/handoffs/260210-knowledge-system-improvement/HANDOFF-2026-02-10-143045.md"
```

Or when passed via @ reference:

```
skill: "load-handoff"
args: "/home/user/workspace/.codex/handoffs/260210-knowledge-system-improvement/HANDOFF-2026-02-10-143045.md"
```

## Handoff File Analysis Checklist

After reading the handoff file, verify:

- [ ] Is Task Overview clear?
- [ ] Are Completed Tasks (✅) and In Progress (🔄) distinguished?
- [ ] Are Identified Issues listed?
- [ ] Does Next Steps contain executable code?
- [ ] Are file paths accurate?
- [ ] Are Resuming Work steps clear?

## Response Template

After analyzing the handoff file, respond in this format:

```markdown
## 📋 Work Context Restored

**Task**: [Task Overview summary]
**Handoff File**: [file path]

### Progress

**Completed (✅)**:
- [Completed task 1]
- [Completed task 2]

**In Progress (🔄)**:
- [In-progress task]

**Pending (⏳)**:
- [Pending task]

### Identified Issues

1. [Issue 1]
2. [Issue 2]

### Next Steps

[Summary of Next Steps content]

---

**Ready to start the next task?**
- Task: [First item in Next Steps]
- File: [Related file path]
```

## Real Example

### Input

```
/load_handoff @HANDOFF-2026-02-10-143045.md
```

Or

```
/load_handoff .codex/handoffs/260210-knowledge-system-improvement/HANDOFF-2026-02-10-143045.md
```

### Response

```markdown
## 📋 Work Context Restored

**Task**: DroidRun Knowledge System Improvement - Quality Management and Semantic Search Implementation
**Handoff File**: .codex/handoffs/260210-knowledge-system-improvement/HANDOFF-2026-02-10-143045.md

### Progress

**Completed (✅)**:
- Knowledge system structure analysis complete
- Added quality tracking fields and methods to models.py
- Ensured JSON compatibility in to_dict(), from_dict()

**In Progress (🔄)**:
- Creating quality.py file - KnowledgeQualityManager class implementation needed

**Pending (⏳)**:
- Add version control to storage.py
- Implement semantic_search.py

### Identified Issues

1. Keyword matching only, cannot capture semantic similarity
2. No feedback loop
3. No version control

### Next Steps

**1. Create KnowledgeQualityManager**

File: `droidrun/knowledge/quality.py`

```python
from typing import List, Optional
from .models import Shortcut, Tip, KnowledgePool

class KnowledgeQualityManager:
    def __init__(self,
                 min_quality_score: float = 0.3,
                 max_failure_count: int = 5,
                 prune_unused_days: int = 90):
        self.min_quality_score = min_quality_score
        self.max_failure_count = max_failure_count
        self.prune_unused_days = prune_unused_days

    def prune_low_quality(self, pool: KnowledgePool) -> int:
        # Pruning logic
        pass
```

---

**Ready to start the next task?**
- Task: Implement KnowledgeQualityManager class
- File: droidrun/knowledge/quality.py
```

## Error Handling

### File Not Found

```
❌ Handoff file not found: .codex/handoffs/YYMMDD-task-name/HANDOFF-xxx.md

Please check the file path. To find available handoff files:

```bash
ls -lt .codex/handoffs/
```
```

### Invalid Handoff Format

```
⚠️ Invalid handoff file format.

Required sections:
- Task Overview
- Current Progress
- Next Steps

Please ensure the file was generated by the handoff skill.
```

### No File Path Provided

```
❌ No handoff file path provided.

Usage:
/load_handoff .codex/handoffs/YYMMDD-task-name/HANDOFF.md
or
/load_handoff @HANDOFF.md

To find available handoff files:
ls -lt .codex/handoffs/
```

## Resume Work Workflow

1. **Load Context**: Read handoff file
2. **Summarize Status**: Provide current situation summary to user
3. **Request Approval**: Ask if Next Steps should be started
4. **Execute Work**:
   - Perform the first Next Steps task
   - Read, modify, or create files as needed
   - Update status after completion
5. **Create New Handoff** (optional): If additional work remains
   - Update progress
   - Write new Next Steps

## Best Practices

- **Use @ File Reference**: Leverage VSCode's file selection UI for convenience
- **Maintain Context**: Be careful not to lose the context of previous work
- **Incremental Progress**: Perform only one Next Steps task at a time
- **Clear Communication**: Clearly explain what work is being done
- **Track Status**: Mark completed tasks with ✅ and update progress

## Notes

- Handoff files are saved in the format `.codex/handoffs/YYMMDD-[task-slug]/HANDOFF-YYYY-MM-DD-HHMMSS.md` (project root's `.codex/` folder)
- When multiple handoff files exist, use the most recent one
- **NEVER** save in `~/.codex/` (global user folder) — only use the project root's `.codex/handoffs/` directory
- After resuming work, if there's additional progress, create a new handoff file to maintain continuity
- When using @ file reference, it's automatically converted to an absolute path before being passed to the skill
