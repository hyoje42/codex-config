# Editor
.vscode/

# Backup area (not synced)
_backup

# Machine-specific real values (git-ignored) — only the .example template in local/ is committed.
# (config.toml.example does not match this pattern, so it stays tracked.)
config.toml
