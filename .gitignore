# Editor
.vscode/

# Python
__pycache__/

# Backup area (not synced)
_backup

# Machine-specific area: ignore everything in local/, commit only the *.example
# templates. New real-value files under local/ are then ignored automatically.
local/*
!local/*.example
