# Codex가 만드는 로컬 디렉터리 (sync 대상 아님)
.codex

# 백업 영역 (_backup 등 _ 로 시작하는 디렉터리)
_*/**

# 머신 종속 실제 파일 방어 — local/ 에는 .example 템플릿만 커밋한다.
# 실제 값을 채운 config.toml 을 local/ 에 두더라도 절대 커밋되지 않도록 한다.
# (config.toml.example 은 이 패턴에 안 걸리므로 그대로 추적된다.)
config.toml
